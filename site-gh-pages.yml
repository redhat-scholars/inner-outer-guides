runtime:
  cache_dir: ./.cache/antora

site:
  title: OpenShift Inner & Outer Loop Development Guides
  start_page: inner-outer-guides:m1:intro.adoc

content:
  sources:
    - url: .
      start_path: docs

asciidoc:
  attributes:
    experimental: true
    page-pagination: true

ui:
  bundle:
    url: ./ui/ui-bundle.zip
  supplemental_files: ./ui/supplemental-ui

output:
  dir: ./gh-pages
